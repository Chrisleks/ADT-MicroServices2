
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access for the application to function.
    // In a production environment, you should secure this with request.auth.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
